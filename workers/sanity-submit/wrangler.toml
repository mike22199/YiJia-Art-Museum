# Cloudflare Worker：義家藝館投稿代理（token 不放前端）
# 部署說明見同資料夾 SETUP.txt

name = "fifi-sanity-submit"
main = "src/index.js"
compatibility_date = "2026-07-01"

# 非機密設定可寫在這裡；SANITY_WRITE_TOKEN 請用 secrets 設定，不要寫進本檔
[vars]
SANITY_PROJECT_ID = "nmwx8mr7"
SANITY_DATASET = "production"
# 逗號分隔；部署後請改成你的 GitHub Pages 網域（可含本機）
ALLOWED_ORIGINS = "https://mike22199.github.io,https://narrativevessel.org,http://127.0.0.1:5500,http://localhost:5500,http://127.0.0.1:8080,http://localhost:8080"
